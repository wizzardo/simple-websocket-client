apply plugin: 'java'

repositories {
    mavenCentral()
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

group = 'com.wizzardo'
version = '0.3-SNAPSHOT'

ext {
    descriptionString = 'Simple web-socket client'
}
apply from: 'gradle/sonatype.gradle'

dependencies {

    testCompile 'junit:junit:4.12'
}